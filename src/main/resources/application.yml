spring:
  application:
    name: career-board
  datasource:
    # Using PostgreSQL details provided
    url: jdbc:postgresql://db-postgresql-nyc3-96131-do-user-18737254-0.m.db.ondigitalocean.com:25060/defaultdb?currentSchema=career_board_schema
    username: doadmin
    password: AVNS_xi9sHcdBBMxWAznCdBb
    driver-class-name: org.postgresql.Driver
    hikari:
      data-source-properties: {}
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        default_schema: career_board_schema
    hibernate:
      ddl-auto: update  # Automatically create/update tables based on entity definitions
  jackson:
    serialization:
      FAIL_ON_EMPTY_BEANS: false
  flyway:
    enabled: false  # Disable Flyway if not needed


management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: always

greeting: "${sm://projects/553556926651/secrets/greeting/versions/1}"

server:
  port: 8081

aws:
  access-key: ${AWS_ACCESS_KEY_ID:dummyAccessKey}      # Default if not set
  secret-key: ${AWS_SECRET_ACCESS_KEY:dummySecretKey}    # Default if not set
  region: ${AWS_REGION:us-east-1}                        # Default region
  s3:
    bucket-name: ${AWS_BUCKET_NAME:dummyBucketName}      # Default bucket name
    presign-duration-minutes: 30

production:
  domain: ${PRODUCTION_DOMAIN:localhost}  # Defaults to localhost if not set
environment: ${ENVIRONMENT:dev}            # Defaults to dev if not set
